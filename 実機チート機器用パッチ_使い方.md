# 実機チート機器用パッチ(RAWコード形式)

各修正を、実機のチート機器・チートソフトで使える「生コード(RAW)」形式にしたものです。ISOファイルを一切改変せず、無改造の正規ディスクのまま修正を適用できます。**アプローチ2bを実機で使える唯一の形式でもあります。**

## ⚠️ 重要な注意事項

**このコードは実機での動作確認ができていません。** 仕様書(ps2rd/CheatDevicePS2の公開ドキュメント)に基づいて作成し、書式が正しいことは確認済みですが、実際の機器・ソフトで正しく動作するかは未検証です。適用は自己責任でお願いします。動作報告(成功・失敗どちらも)をいただけると、今後の改善に役立ちます。

## 収録ファイル

| ファイル | 内容 | 行数の目安 |
|---|---|---|
| `eikan_koshien_hasha_v1.1_raw.txt` | 基本の不具合修正3件(下記1〜3)。v1.1から内容変更なし | 8行 |
| `eikan_koshien_hasha_approach2a_2b_combined_raw.txt` | **達成レベル修正 2a+2b の完全版**(下記4) | 約190行 |
| `eikan_koshien_hasha_approach2b_raw.txt` | アプローチ2b単体(2aと必ず併用) | 約170行 |
| `eikan_koshien_hasha_approach2a_precise_raw.txt` | アプローチ2a単体 | 約10行 |
| `eikan_koshien_hasha_obgift_guaranteed_raw.txt` | OB機材贈呈の修正(下記5) | 2行 |

> **2a+2b の完全版は約190行あります。** 対応できる行数は機器・ソフトによって異なります(物理カートリッジ系は上限が厳しめ、ホームブリューのCheatDevicePS2は余裕があります)。行数上限に引っかかる場合は、まず `eikan_koshien_hasha_approach2a_precise_raw.txt`(2aのみ、約10行)だけでも「甲子園出場を全く反映しない」最悪ケースは緩和されます。

## 含まれる修正内容

1. 練習試合の対戦相手が池田高校に固定されるバグの修正
2. 組んでいない相手校との練習試合(幽霊試合)が発生するバグの修正
3. 年間目標の達成判定バグの修正(目標が「飛び級」して表示される不具合)
4. **(v1.2)** 達成レベルが実戦績と乖離し意味不明な年間目標が出る問題の修正
   - 2a: 夏の甲子園抽選で自チーム選出時、達成レベルを「甲子園出場」(5)未満なら5に引き上げ
   - 2b: シーズン終了時にトーナメント表から最高進出段階を読み直し、達成レベルを補正(地方1〜4 / 甲子園5〜9)。壊れた10以上の値は上書き、正当な値は引き上げのみ
   - ※2a・2bはセット運用。年間目標の進行を変えます。バニラ挙動を望む場合は当てないでください
   - ※現バージョンは段階1〜9まで対応
5. **(v1.2)** OB機材贈呈が目標達成年でも確率で発生しないことがある問題の修正
   - 達成年の確率ロールを外し、達成していれば(未入手アイテムが残る限り)必ず発生。未達成年は従来どおり非発生

## コードの意味

各行は `アドレス 値` の形式です。先頭が「2」で始まるアドレスは、「その32bit値を対象アドレスへ継続的に(毎フレーム)書き込む」という意味の生コードです。既存のプログラム命令をこの値で置き換える(バグの原因命令を正しい命令に差し替える)、またはアプローチ2bの場合はゲーム未使用のメモリ領域(`0x01F00000`〜)へルーチン本体を毎フレーム書き込むために使っています。

**アプローチ2bのコードは順序どおり全行を投入してください。** 一部だけだとルーチンが不完全になり、達成レベルが誤って書き換わる恐れがあります。行数上限で全部入らない機器では、2bは使わず2a単体(または当てない)にしてください。

## 推奨する使い方: PS2ホームブリュー「Cheat Device」経由(推奨)

物理的なチート機器の購入は不要です。実機PS2用パッチの案内(`実機PS2・PS3用パッチ_使い方.md`)と同じ **Free McBoot(FMCB)+ Open PS2 Loader(OPL)** の環境に、追加で以下を導入してください。

1. ホームブリュー「**Cheat Device**」([root670/CheatDevicePS2](https://github.com/root670/CheatDevicePS2) または [israpps版](https://github.com/israpps/CheatDevicePS2))を導入
2. 使いたい`_raw.txt`の内容を、Cheat Deviceが読み込める場所に配置(ツールのドキュメントに従ってください)
3. 通常通り(無改造の)ディスクまたはISOでゲームを起動し、Cheat Device経由でコードを有効化

## 物理カートリッジ(CodeBreaker/Action Replay等)をお持ちの場合

上記のコードは無暗号化(RAW/V1形式)です。**CodeBreaker実機**で使う場合、コードリストの先頭に暗号化を無効化する特殊コード

```
FFFFFFFF 00000000
```

を追加する必要がある場合があります(バージョンにより挙動が異なる可能性があります)。Action Replay MAX等、他の機器については、それぞれの機器がRAWコード形式の読み込みと必要な行数に対応しているか、機器の説明書を確認してください。**アプローチ2bの約190行はカートリッジ系では入りきらない可能性が高いです。**

## 検証状況

- 書式・アドレス計算: PCSX2用パッチ(検証済み)から機械的に変換、内容の一致を確認済み
- アプローチ2bのルーチンは、PCSX2上でセーブステート+メモリ注入による13ケースのテストで全パス(pnach版と同一バイト列)
- **実機での動作: 未検証**(動作報告募集中)
