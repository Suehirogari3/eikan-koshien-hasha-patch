# 実機チート機器用パッチ(RAWコード形式)

`eikan_koshien_hasha_v1.1_raw.txt` は、練習試合バグ修正パッチを、実機のチート機器・チートソフトで使える「生コード(RAW)」形式にしたものです。ISOファイルを一切改変せず、無改造の正規ディスクのまま修正を適用できます。

## ⚠️ 重要な注意事項

**このコードは実機での動作確認ができていません。** 仕様書(ps2rd/CheatDevicePS2の公開ドキュメント)に基づいて作成し、書式が正しいことは確認済みですが、実際の機器・ソフトで正しく動作するかは未検証です。適用は自己責任でお願いします。動作報告(成功・失敗どちらも)をいただけると、今後の改善に役立ちます。

## 含まれる修正内容

PCSX2用パッチ・実機ISO用IPSパッチと同一の、以下3件の修正です。

1. 練習試合の対戦相手が池田高校に固定されるバグの修正
2. 組んでいない相手校との練習試合(幽霊試合)が表示されるバグの修正
3. **(v1.1で追加)** 年間目標の達成判定バグの修正(目標が「飛び級」して表示される不具合)

## コードの意味

各行は `アドレス 値` の形式です。先頭が「2」で始まるアドレスは、「その32bit値を対象アドレスへ継続的に(毎フレーム)書き込む」という意味の生コードです。今回は既存のプログラム命令をこの値で置き換える(=バグの原因になっている命令を正しい命令に差し替える)ために使っています。

## 推奨する使い方: PS2ホームブリュー「Cheat Device」経由(推奨)

物理的なチート機器の購入は不要です。実機PS2用パッチの案内(`実機PS2・PS3用パッチ_使い方.md`)と同じ **Free McBoot(FMCB)+ Open PS2 Loader(OPL)** の環境に、追加で以下を導入してください。

1. ホームブリュー「**Cheat Device**」([root670/CheatDevicePS2](https://github.com/root670/CheatDevicePS2) または [israpps版](https://github.com/israpps/CheatDevicePS2))を導入
2. `eikan_koshien_hasha_v1.1_raw.txt` の内容を、Cheat Deviceが読み込める場所に配置(ツールのドキュメントに従ってください)
3. 通常通り(無改造の)ディスクまたはISOでゲームを起動し、Cheat Device経由でコードを有効化

## 物理カートリッジ(CodeBreaker/Action Replay等)をお持ちの場合

上記のコードは無暗号化(RAW/V1形式)です。**CodeBreaker実機**で使う場合、コードリストの先頭に暗号化を無効化する特殊コード

```
FFFFFFFF 00000000
```

を追加する必要がある場合があります(バージョンにより挙動が異なる可能性があります)。Action Replay MAX等、他の機器については、それぞれの機器がRAWコード形式の読み込みに対応しているか、機器の説明書を確認してください。

## 検証状況

- 書式・アドレス計算: PCSX2用パッチ(検証済み)から機械的に変換、内容は一致を確認済み
- 実機での動作: **未検証**(動作報告募集中)
